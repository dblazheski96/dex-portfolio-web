export type LogoItemDomain = {
  [id: string]: {
    img: string
    imgPopup: string
    orderIndex: number
  }
}

export type LogoItem = {
  id: string
  img: string
  imgPopup: string
  orderIndex: number
}